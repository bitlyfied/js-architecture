tks.component('sortable-table', function(core){
  
  // we could even remove this and use it as a default
  core.register('sortable-table', '[data-component=sortable-table]');

  core.on('ready', function(){
    // initialization required on dom ready
    // for all the plugins
  });

  return function CarouselComponent(){

    this.init = function(){

    }

    this.next = function(){

    }
  };

});